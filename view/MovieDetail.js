import React, { Component, PropTypes } from 'react';
import { View, Text, StyleSheet, Image } from 'react-native';

export default class MovieDetail extends Component {
  render() {
    return <Text>some movie</Text>;
  }
}
